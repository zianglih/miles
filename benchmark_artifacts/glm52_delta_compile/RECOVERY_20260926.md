# Recovery after the 2026-09-26 01:59 UTC devbox deletion

The deleted devbox used an emptyDir for `/hai-workspace`; it was not durable.
Original PR validation and final CPU replays are retained locally. The new v1
NumPy arm is incomplete local evidence and the compiled arm was interrupted.
Do not infer missing raw records from summaries or mix them into a replacement
pair. Leave original `CAMPAIGN.json`, `environment-image.json`, archived plans and
artifacts unchanged. A new plan must pin its own campaign/environment metadata.

The replacement uses `radixark/miles:dev-202609252029`, C2 namespace `ziangli`,
devbox `glm52-delta-0926`. Record its resolved digest, host and runtime versions;
the old image's package versions are historical, not replacement attestation.

## Restore the retained sources and helpers

`recovery-20260926/sources/` contains small shallow Git stores retaining exact
commits and complete source trees, with no parent-history dependency:

| Checkout | Exact commit |
| --- | --- |
| miles | f17ba4bce13bf7d357e7560182dc859c41a7cb37 |
| miles-torch-compile | 6fb0e2a9d81fc343863ba6015512fe93992892e4 |
| miles-torch-compile-validation | 283a1cb4e6ed4a3712a39bc5dbd26060465bdcca |
| sglang | 2f5fb2a09f08eb9a44c0fed5a28be0bda2942b0f |

The restore script creates independent project-local Git checkouts, verifies all
four heads and clean state, and copies only hash-listed helpers. Existing correct
files are reused; differing files are rejected. The frozen measurement helpers
are copied byte-for-byte. Source stores passed `git fsck --full` locally.

```bash
python recovery-20260926/restore_sources.py --root /hai-workspace/glm52-delta
git -C /root/Megatron-LM rev-parse HEAD
```

The retained Megatron head is `f148a32b4385b758b66a77c9c3ad1641f1295d4b`.
If the new image differs, resolve the source overlay explicitly before conversion;
do not silently mutate `/root/Megatron-LM`. Source restoration needs no GPU.
The independent Torch 2.11 hosted-CI diagnosis uses
`/hai-workspace/glm52-delta-ci-torch211`; do not touch its environment or sources.

## Look for reusable inputs before downloading

Inspect mounts and bounded task-owned locations under `/data/ziangli`,
`/scratch/ziangli`, `/data/models`, `/scratch/models`, `/root/models` and the local
HF cache. A same-host `/scratch` copy might survive, but a different node's scratch
is not shared. Avoid a recursive scan of the full multi-petabyte `/data` mount.
Use `find` with an explicit candidate parent and `-maxdepth 4` for directories named
`GLM-5.2_5layer`, `GLM-5.2_5layer-NVFP4`, `GLM-5.2_5layer_torch_dist`, or `dapo-math-17k`.
Prefer `/data/ziangli/glm52-delta-sync-c2/prepared-inputs` if it exists.

Copy a candidate into the task's new `models/` or `datasets/` directory only when
the destination does not exist. Never follow an existing `models/<name>` symlink
into a shared writable checkpoint. Retain the candidate unchanged. The recovery
helper verifies exact Hub model hashes and the retained dataset bytes before use.
Old path-only conversion markers are not adequate proof of conversion contents;
unknown converted directories are retained under `.unverified-<timestamp>` before
reconversion. New hash-verified completion markers make later recovery resumable.

## Exact download and conversion

- Model: `Pinaster/GLM-5.2_5layer`, revision
  `1c749139f70e158e4420ba67f342bef1de2e650d`.
- Dataset: `zhuzilin/dapo-math-17k`, revision
  `2e65612930298bde4c5d58fd97b3f23a483aaff9`.
- Required `dapo-math-17k.jsonl`: 10,490,834 bytes, SHA256
  `cc9c39c2aa19177abe9464741e121cf4cac90fd25484ef3cdf86535101e3a5b6`.
- Native model config: `glm_moe_dsa`, `GlmMoeDsaForCausalLM`, five layers, no
  `auto_map`. Retained revision records and canonical index/config files accompany
  the bundle under `artifacts/`; they are provenance, not substitutes for weights.

The old `prepare_weight_sync.py` pins only the model, so do not run it directly for
recovery. The separate helper below also pins and hashes the dataset and source
model, retains each failed attempt's log/exit, and keeps all existing artifacts.
The same f17 recipe supplies every NVFP4 environment flag and shared-expert BF16
exclusion. Torch-dist conversion uses static localhost rendezvous at port 29507,
avoiding the previous unresolved-hostname failure. Both conversions use TP/PP/EP/
ETP 1, matching the original preparation; the training recipe later uses 4+4.

```bash
cd /hai-workspace/glm52-delta
/opt/sglang/bin/python recover_weight_sync_inputs.py --download-only
/opt/sglang/bin/python recover_weight_sync_inputs.py \
  --backup-dir /data/ziangli/glm52-delta-sync-c2/prepared-inputs
```

The first command can run during source/CPU-diagnostic setup. Run conversion only
in the granted model-preparation slot. Repeating the second command verifies and
reuses completed stages. All output file hashes are recorded per conversion.
Package versions are recorded rather than assumed equal to the deleted image.
No training or benchmark is launched by this helper.

The durable backup runs only after input/conversion verification, checks free
capacity, copies every prepared file through a temporary path, verifies SHA256,
and records the full manifest. Existing identical backup files are reused;
different files cause an error. If space is inadequate, local preparation remains
intact and the backup failure is explicit. Preserve logs and verification manifests
on `/data/ziangli/glm52-delta-sync-c2/setup` after the stage completes too.
Measured delta publication remains on `/hai-workspace`; `/data` is for recovery
copies, not timed transport.

Before the replacement pair, finish the isolated CPU diagnosis, record the new
environment/calibration and active plan, and wait for root's timing-slot release.
Use fresh run/cache names. The runner's per-arm durable archive executes only after
that arm stops and summaries complete, before the next arm. Do not overlap copying,
hashing, compilation diagnostics or setup with measured GPU updates.
